---
import "@fontsource-variable/urbanist";
import "../styles/global.css";
import Callout from "../components/callout.astro";
import Layout from "../layouts/default.astro";

const headTitle = "Astro Party!! 🎉 🥳";
const pageTItle = "It's A Freakin' Astro Party, Dude! 🎉 🥳";
---

<Layout headTitle={headTitle} pageTitle={pageTItle}>
	<h1>{pageTItle}</h1>

	<Callout icon="🔥" iconDescription="fire">
		<p>Oooh, dang, bruv! Lookkit dis! 👀</p>
	</Callout>
</Layout>

<style>
	header {
		align-items: center;
		background-color: #005;
		color: #ddd;
		display: flex;
		justify-content: space-between;
		padding: 1rem max(5dvi, 2rem);

		nav {
			display: flex;
			gap: 1rem;
		}

		a {
			color: inherit;
			text-decoration: none;
		}
	}

	main {
		inline-size: min(800px, 90dvi);
		margin-inline: auto;
		padding: 3rem 0;
	}
</style>
